"""Rejestr domain exceptions — imported from core.errors.

Domain code imports from here; core.errors is the single source of truth.
"""

from mcp_emp.core.errors import InvalidTransition as InvalidTransition  # noqa: PLC0414
from mcp_emp.core.errors import TaskNotFound as TaskNotFound  # noqa: PLC0414
